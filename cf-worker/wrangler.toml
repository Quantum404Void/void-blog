name = "wandbox-proxy"
main = "src/index.js"
compatibility_date = "2024-01-01"

# KV namespace for rate limiting (可选，不配置则跳过速率限制)
# 部署后在 CF dashboard 创建 KV namespace 并绑定，命名为 RATE_KV
# [[kv_namespaces]]
# binding = "RATE_KV"
# id = "YOUR_KV_NAMESPACE_ID"
