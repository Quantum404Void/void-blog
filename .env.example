# void-blog-nuxt 环境变量
# D1 通过 Cloudflare Pages binding 直接访问，无需 API Key。
# 本地开发：wrangler pages dev dist --d1=void_blog_posts
# 部署：Cloudflare Pages Dashboard → Settings → D1 bindings 绑定 void_blog_posts

# 如需其他服务（如 Resend 邮件等），在此添加：
# NUXT_SOME_SECRET=
